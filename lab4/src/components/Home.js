import React from 'react';
import '../App.css';

const Home = () => {
  return (
    <div>
      <p>
        This is a simple example of using React to Query the Marvel API. Start
        by clicking the "Characters" or "Series" or "Comics" button above
      </p>
    </div>
  );
};

export default Home;
